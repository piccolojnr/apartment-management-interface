import React from "react";
import { Card, CardContent, Typography } from "@mui/material";

const Reports = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant="h5">Reports</Typography>
        <Typography color="textSecondary">
          Graphs and charts will be here
        </Typography>
      </CardContent>
    </Card>
  );
};

export default Reports;
